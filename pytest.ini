[pytest]
# `arc` is not installed and there is no tests/__init__.py, so the suite only
# resolves with the repo root on sys.path. This makes a bare `pytest` work from
# any directory. Preferred over `pip install -e .`, whose
# find_packages(include=["arc"]) matches only the top-level package.
pythonpath = .
testpaths = tests
